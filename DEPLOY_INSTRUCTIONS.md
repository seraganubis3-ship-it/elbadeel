# 🚀 دليل رفع الموقع على السيرفر (VPS)

لقد قمت بإنشاء ملف `release.zip` يحتوي على كل ما تحتاجه (بدون الملفات الثقيلة). اتبع الخطوات التالية:

## 1. رفع الملف

ارفع ملف `release.zip` إلى السيرفر في مجلد جديد، مثلاً `/var/www/my-app`.

## 2. فك الضغط

```bash
# لو السيرفر Ubuntu
sudo apt-get install unzip
unzip release.zip
```

## 3. تثبيت الاعتماديات

```bash
npm install
```

## 4. إعداد قاعدة البيانات

1. أنشئ ملف `.env` جديد (انسخ محتويات .env المحلي) وضع فيه رابط قاعدة البيانات الحقيقي.

```bash
nano .env
# DATABASE_URL="postgresql://..."
```

2. نفذ الأوامر التالية لتجهيز القاعدة:

```bash
npm run db:generate
npm run db:deploy  # هذا الأمر ينشئ الجداول في القاعدة الحقيقية
npm run db:seed    # لإضافة البيانات الأولية
```

## 5. بناء المشروع

```bash
npm run build
```

## 6. التشغيل (Production Mode)

يفضل استخدام `pm2` لضمان عمل الموقع والواتساب بوت بشكل دائم.

```bash
# تثبيت pm2
npm install -g pm2

# تشغيل الموقع (Next.js)
pm2 start npm --name "website" -- start

# تشغيل البوت (WhatsApp)
pm2 start whatsapp-bot.js --name "whatsapp-bot"

# حفظ التشغيل التلقائي عند إعادة التشغيل
pm2 save
pm2 startup
```

---

💡 **ملاحظة:** تأكد أن المنفذ `3000` (للموقع) و `3001` (للبوت) مفتوحين في الـ Firewall.

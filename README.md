# 🚀 منصة الباديل - Albadil Platform

منصة موثوقة وسريعة لاستخراج جميع أنواع الأوراق الرسمية في مصر

## ✨ المميزات

- 🗄️ **قاعدة بيانات PostgreSQL** متقدمة مع indexes محسنة
- 🔒 **SSL/HTTPS** مع شهادات Let's Encrypt
- 👥 **نظام مستخدمين متكامل** مع صلاحيات مختلفة
- 📋 **إدارة الطلبات** مع تتبع الحالة
- 💰 **نظام مدفوعات** متعدد الطرق
- 📊 **تقارير متقدمة** مع رسوم بيانية تفاعلية
- 📱 **تصميم متجاوب** لجميع الأجهزة
- 🔍 **SEO محسن** مع metadata شامل
- 📧 **نظام بريد إلكتروني** للتواصل

## 🛠️ المتطلبات التقنية

- **Node.js**: 18.0.0 أو أحدث
- **PostgreSQL**: 15 أو أحدث
- **Docker**: 20.10 أو أحدث
- **Nginx**: (للإنتاج)
- **Ubuntu**: 20.04 LTS أو أحدث (للإنتاج)

## 🚀 التثبيت والتشغيل

> **ملاحظة مهمة**: تم توحيد قاعدة البيانات لاستخدام PostgreSQL فقط. لمزيد من التفاصيل، راجع ملف `DATABASE_SETUP.md`.

### 1. تثبيت التبعيات

```bash
# نسخ المشروع
git clone <repository-url>
cd albadil-platform

# تثبيت التبعيات
npm install
```

### 2. إعداد قاعدة البيانات PostgreSQL

```bash
# تشغيل قاعدة البيانات باستخدام Docker
npm run docker:up

# انتظار حتى تعمل قاعدة البيانات
npm run docker:logs

# إنشاء Prisma Client
npm run db:generate

# تطبيق Schema على قاعدة البيانات
npm run db:push

# إدخال البيانات الأولية
npm run db:seed

# أو استخدام الأمر المجمع للإعداد السريع
npm run setup:dev
```

### 3. إعداد متغيرات البيئة

```bash
# نسخ ملف البيئة
cp .env.example .env.local

# تعديل المتغيرات
nano .env.local
```

**محتوى ملف .env.local:**
```env
# Database
DATABASE_URL="postgresql://albadil_user:albadil_password_2024@localhost:5432/albadil_prod"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-development-secret-key"

# Email
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
```

### 4. تشغيل التطبيق

```bash
# وضع التطوير
npm run dev

# أو بناء وتشغيل للإنتاج
npm run build
npm run start
```

## 🗄️ قاعدة البيانات

تم توحيد قاعدة البيانات لاستخدام **PostgreSQL** فقط، وهو الخيار الأفضل للإنتاج والتطوير.

### المميزات
- 🚀 **أداء عالي** مع فهارس محسنة
- 🌐 **دعم كامل للغة العربية** مع فهرسة نصية
- 📊 **نظام تدقيق شامل** لجميع العمليات
- 🔒 **أمان متقدم** مع صلاحيات مفصلة
- 📁 **دعم الملفات المتعددة** مع تخزين آمن

### الهيكل

- **User**: المستخدمين مع الصلاحيات
- **Category**: فئات الخدمات
- **Service**: الخدمات المتاحة
- **ServiceVariant**: أنواع الخدمات (عادي، سريع، عاجل)
- **Order**: طلبات العملاء
- **Payment**: المدفوعات
- **AuditLog**: سجل العمليات
- **SystemSettings**: إعدادات النظام

### الأداء

- **Indexes محسنة** للبحث السريع
- **Full-text search** للخدمات والمستخدمين
- **Connection pooling** مع Prisma
- **Query optimization** مع indexes مركبة

## 🔒 الأمان

### SSL/HTTPS

```bash
# إعداد SSL (للإنتاج فقط)
chmod +x setup-ssl.sh
sudo ./setup-ssl.sh
```

### الأمان

- **JWT tokens** مع NextAuth.js
- **Password hashing** باستخدام bcrypt
- **Role-based access control**
- **Input validation** مع Zod
- **SQL injection protection** مع Prisma
- **XSS protection** مع Content Security Policy

## 📊 التقارير والإحصائيات

### لوحة التحكم

- **إحصائيات عامة**: المستخدمين، الطلبات، الإيرادات
- **تقارير متقدمة**: حسب الفترة الزمنية
- **رسوم بيانية تفاعلية**: باستخدام Recharts
- **تصدير البيانات**: CSV/Excel

### API Endpoints

- `GET /api/admin/reports` - التقارير
- `GET /api/admin/users` - إدارة المستخدمين
- `GET /api/admin/orders` - إدارة الطلبات
- `POST /api/orders` - إنشاء طلب جديد

## 🚀 النشر للإنتاج

### 1. إعداد الخادم

```bash
# تحديث النظام
sudo apt update && sudo apt upgrade -y

# تثبيت Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# تثبيت Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

### 2. نشر التطبيق

```bash
# نسخ المشروع
git clone <repository-url>
cd albadil-platform

# إعداد قاعدة البيانات
npm run production:setup

# بناء التطبيق
npm run build

# تشغيل التطبيق
npm run production:start
```

### 3. إعداد Nginx

```bash
# نسخ ملف التكوين
sudo cp nginx.conf /etc/nginx/sites-available/albadil

# تفعيل الموقع
sudo ln -s /etc/nginx/sites-available/albadil /etc/nginx/sites-enabled/

# اختبار التكوين
sudo nginx -t

# إعادة تحميل Nginx
sudo systemctl reload nginx
```

## 🗄️ أوامر قاعدة البيانات

### الأوامر الأساسية
```bash
# إنشاء Prisma Client
npm run db:generate

# تطبيق Schema على قاعدة البيانات
npm run db:push

# إنشاء وتطبيق المايجريشن
npm run db:migrate

# إدخال البيانات الأولية
npm run db:seed

# فتح Prisma Studio
npm run db:studio

# إعادة تعيين قاعدة البيانات
npm run db:reset

# نشر المايجريشن للإنتاج
npm run db:deploy

# فحص حالة المايجريشن
npm run db:status
```

### أوامر Docker
```bash
# تشغيل قاعدة البيانات
npm run docker:up

# إيقاف قاعدة البيانات
npm run docker:down

# إعادة تشغيل
npm run docker:restart

# عرض السجلات
npm run docker:logs

# تنظيف وإعادة إنشاء
npm run docker:clean
```

### أوامر الإعداد السريع
```bash
# إعداد بيئة التطوير
npm run setup:dev

# إعداد بيئة الإنتاج
npm run setup:prod
```

## 🔧 الصيانة

### نسخ احتياطي

```bash
# نسخ احتياطي لقاعدة البيانات
docker exec albadil_postgres pg_dump -U albadil_user albadil_prod > backup_$(date +%Y%m%d_%H%M%S).sql

# استعادة نسخة احتياطية
docker exec -i albadil_postgres psql -U albadil_user -d albadil_prod < backup_file.sql
```

### التحديثات

```bash
# سحب التحديثات
git pull origin main

# تثبيت التبعيات الجديدة
npm install

# إعادة بناء التطبيق
npm run build

# إعادة تشغيل التطبيق
pm2 restart albadil
```

## 📱 الواجهات

### الصفحات الرئيسية

- **الصفحة الرئيسية**: عرض الخدمات والمميزات
- **صفحة الخدمات**: قائمة شاملة بجميع الخدمات
- **صفحة FAQ**: الأسئلة الشائعة
- **صفحة الاتصال**: معلومات التواصل

### لوحة التحكم

- **Dashboard**: إحصائيات عامة
- **إدارة المستخدمين**: إضافة، تعديل، حذف المستخدمين
- **إدارة الطلبات**: تتبع وتحديث حالة الطلبات
- **التقارير**: إحصائيات متقدمة ورسوم بيانية
- **إدارة الخدمات**: إضافة وتعديل الخدمات

## 🎨 التصميم

### التقنيات المستخدمة

- **Next.js 14**: React framework
- **Tailwind CSS**: تصميم متجاوب
- **TypeScript**: برمجة آمنة
- **Prisma**: ORM متقدم
- **NextAuth.js**: نظام مصادقة
- **Recharts**: رسوم بيانية تفاعلية

### المميزات

- **تصميم متجاوب**: يعمل على جميع الأجهزة
- **واجهة عربية**: تصميم مخصص للغة العربية
- **ألوان متناسقة**: نظام ألوان موحد
- **انيميشن**: انتقالات سلسة
- **أيقونات**: مجموعة شاملة من الأيقونات

## 📞 الدعم

### معلومات الاتصال

- **Email**: admin@albadil.com
- **Phone**: +20 10 2160 6893
- **Address**: فيصل - جيزة، مصر
- **Working Hours**: الأحد - الخميس: 9:00 ص - 6:00 م

### الوثائق

- [دليل النشر](DEPLOYMENT.md)
- [إعداد قاعدة البيانات](DATABASE_SETUP.md)
- [دليل API](docs/API.md)
- [دليل المستخدم](docs/USER_GUIDE.md)
- [دليل المدير](docs/ADMIN_GUIDE.md)

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT. راجع ملف [LICENSE](LICENSE) للتفاصيل.

## 🤝 المساهمة

نرحب بمساهماتكم! يرجى قراءة [دليل المساهمة](CONTRIBUTING.md) قبل البدء.

---

**ملاحظة**: تأكد من اختبار كل خطوة قبل الانتقال للخطوة التالية. في حالة وجود أي مشاكل، راجع السجلات ووثائق البرامج المستخدمة.
